Generated snapshot of injected scripts from
`entry/src/main/ets/constants/AppConstants.ets`.

Do not edit these files. After changing AppConstants:

```bash
cd core
npm run export-js
```

CI runs `npm run export-js:check`.
