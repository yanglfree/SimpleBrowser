{
  "name": "zhuobrowser-mobile-distribution",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify:worker": "wrangler deploy --dry-run --config wrangler.jsonc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "4.127.1"
  }
}
