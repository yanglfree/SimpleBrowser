import SwiftUI

/// Light tokens from `core/tokens/color.json` / DESIGN.md.
enum DesignTokens {
    static let pageBackground = Color(red: 0xFA / 255, green: 0xF9 / 255, blue: 0xF7 / 255)
    static let surfacePanel = Color.white
    static let surfaceSubtle = Color(red: 0xF1 / 255, green: 0xEF / 255, blue: 0xEA / 255)
    static let textPrimary = Color(red: 0x1A / 255, green: 0x1A / 255, blue: 0x18 / 255)
    static let textSecondary = Color(red: 0x8B / 255, green: 0x87 / 255, blue: 0x7F / 255)
    static let border = Color(red: 0xE5 / 255, green: 0xE2 / 255, blue: 0xDB / 255)
    static let accent = Color(red: 0x2E / 255, green: 0x6B / 255, blue: 0x5C / 255)
}
