# Zhuoyue Browser for Android

Not started. Phone V1 will be Kotlin + Jetpack Compose + system `WebView`,
sharing scripts and contracts from `../core` the same way `../ios` does.

HarmonyOS stays in `../ohos`. Do not rewrite that client in Flutter.
